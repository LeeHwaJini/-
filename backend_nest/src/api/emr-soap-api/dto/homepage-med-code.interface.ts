export interface HomepageMedCode {
  ArrayOfHomepageMedCode: {
    $: {
      "xmlns:xsd": string;
      "xmlns:xsi": string;
      xmlns: string;
    };
    HomepageMedCode: [{ CDCODE: [string]; CDVALUE: [string] }];
  };
}
